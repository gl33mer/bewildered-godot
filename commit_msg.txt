Fix Stage 4 Invalid Move Rejection & Board Sync Bugs

- Fixed `_animate_clear()` to immediately set `gem_instances[idx] = null` when starting clear animation
- Fixed `_animate_rejection_snapback()` to properly call `_sync_board_state()` after rejection animation
- Added `_force_unlock_rejection()` helper that forcibly resets `is_animating`, `is_processing_swap`, `selected_cell`, and calls `_sync_board_state()`
- Added 0.5s safety timeout timer in `_animate_rejection_snapback()` that triggers `_force_unlock_rejection()` if tween hangs
- Fixed `safety_timer.stop()` error (SceneTreeTimer doesn't have stop())
- Enhanced `_sync_board_state()` to guarantee all 64 cells have valid gems
- Refactored `_try_swap_cells()` to chain `_sync_board_state()` after swap animation
- Rejection snap-back now chains to `_sync_board_state()` via tween chain

Verification:
- COORD SELF-TEST: ALL PASSED (9/9)
- Invalid swap rejection no longer freezes input
- Board stays synchronized with BoardSim (64 gems maintained)
- No 'Method not found' or 'stop() on SceneTreeTimer' errors
- Multiple cascades complete correctly with board staying full (64 gems)
- No script errors during runtime